# docker-compose -f icd.yml up -d
version: '2.4'
services:

  # Ergo (formerly known as Oragono) - IRC server written in Go
  # https://ergo.chat/
  # https://github.com/ergochat/ergo
  # https://github.com/ergochat/ergo/blob/master/docs/MANUAL.md
  # https://github.com/ergochat/ergo/blob/master/distrib/docker/README.md
  # Supported architectures: amd64, arm64, ...
  # Oper Credentials see in logs after first start: docker logs ergo 2>&1 | grep 'Oper username:password'
  # After first start change settings in config in ./data_ircd/ircd.yaml and "/rehash" in chat or restart container or "docker kill -s SIGHUP ergo".
  ircd:
    image: ghcr.io/ergochat/ergo:stable  # v2.11.1
    container_name: ircd
    restart: unless-stopped
    ports:
      - 6667:6667/tcp
      - 6697:6697/tcp  # TLS
      #- 8067:8067/tcp  # Websocket
      #- 8097:8097/tcp  # Websocket TLS
    volumes:
      - ./data_ircd:/ircd:rw
    healthcheck:
      test: ["CMD-SHELL", "nc -nzv -w 3 127.0.0.1 6697 || exit 1"]
      interval: 120s
      timeout: 5s
      retries: 3
      #start_period: 30s

#  # https://github.com/dgl/ircd_exporter
#  ircd-exporter:
#    image: tobi312/tools:ircd-exporter
#    build:
#      context: https://raw.githubusercontent.com/Tob1as/docker-tools/main/ircd-exporter.Dockerfile
#      dockerfile: ircd-exporter.Dockerfile
#      #args:
#      #  GOLANG_VERSION: 1.21
#      #  BUILD_DATE: 2023-10-01
#    container_name: ircd-exporter
#    restart: unless-stopped
#    #ports:
#    #  - 127.0.0.1:9678:9678/tcp
#    command:
#      - "-irc.nick=promexp"
#      #- "-irc.oper=admin"
#      #- "-irc.oper-password="
#      #- "-irc.password="
#      - "-irc.server=ircd:6667"
#      #- "-listen=:9678"
#      - "-stats.command-usage"
#      #- "-stats.ignore="
#      - "-stats.local-only"
#      #- "-stats.nicks="
#      #- "-stats.timeout=9s"
#    depends_on:
#      - ircd

networks:
  default:
    external: true
    name: mynetwork
